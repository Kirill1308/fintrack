spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://${HOST}:5432/${POSTGRES_DATABASE}?currentSchema=${POSTGRES_TEST_SCHEMA}
    username: postgres
    password: postgres
  liquibase:
    enabled: false
